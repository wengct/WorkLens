# WorkLens 網站風格設計規範

本文件規範 WorkLens 主應用程式的視覺風格、版面、元件與互動呈現，供新增頁面、調整 UI 及審查介面時使用。以 [wwwroot/app.css](wwwroot/app.css)、[MainLayout.razor](Components/Layout/MainLayout.razor) 與既有共用元件為實作依據；[screenshot/](screenshot/) 提供介面參考，實際樣式仍以程式為準。

適用範圍為 `Components/` 與 `wwwroot/` 的 Blazor 網站。`site/` 為獨立產品介紹網站，修改時應參照其既有樣式，不直接套用本文件的側欄與工作台尺寸。本文件不描述後端系統架構。

### 如何使用本規範

先讀視覺方向，再依任務查閱色彩、版面及元件章節，最後用驗收流程確認結果。標示「目前／既有基準」的數值描述現況；「應／須／不得」是新增及修改介面的要求，並不代表既有頁面已全部通過驗收。局部特例只適用於註明的元件。

若文件與 CSS 不一致，先確認是否為刻意的頁面特例或文件過期，再同步修正；不要為了符合文件而直接重設全站樣式。本文件選取可重用的設計決策，不逐條複製 CSS。新的決策應同時說明外觀、使用情境、理由及驗證方式。

## 1. 視覺方向

採用清晰、沉穩、適合長時間工作的工具型介面：深藍導覽、淺灰藍背景、白色面板，以靛藍突出主要操作。資訊密度適中，優先讓使用者辨識「目前位置、工作內容、下一步操作及執行結果」。

- 維持淺色內容區與深色導覽的既有組合；目前沒有完整深色主題規範。
- 使用細邊框、柔和陰影及中等圓角區分層級；漸層僅限既有功能提示區等局部用途。
- 同一操作區以一個主要動作為優先，其餘採次要按鈕、文字連結或更多選單。
- 長篇工作內容保留閱讀空間，設定與狀態資訊採較緊湊排列。
- 新增畫面先重用既有 class 與共用元件，避免建立外觀不同但用途相同的控制項。

冷色背景讓白色工作區自然成為閱讀焦點，深色側欄提供穩定的定位感。主色集中在可操作與已選取的元素，讓工作正文保有視覺優先權。面板的柔和圓角與低對比陰影用來區分內容群組；只有對話框等覆蓋層使用較強陰影表達前後關係。

## 2. 色彩

優先使用 `:root` 既有 CSS 變數。以下為目前主題基準；新增共用色彩時應集中定義，避免在個別頁面散落近似色碼。

| 用途 | 變數／目前色值 | 使用規則 |
| --- | --- | --- |
| 主要文字 | `--ink: #24324a` | 正文、輸入內容及重要資訊。 |
| 次要文字 | `--muted: #6e7b91` | 說明、時間與輔助資訊；關鍵內容不要過度淡化。 |
| 頁面背景 | `#f4f6fb` | 主內容區的底色。 |
| 面板背景 | `--panel: #ffffff` | 卡片、表單及主要內容容器。 |
| 分隔與邊框 | `--line: #e1e6f0` | 面板邊框、區段分隔。 |
| 主色 | `--primary: #5668d8` | 主要按鈕、連結與選取強調。 |
| 主色 hover | `--primary-dark: #4050b7` | 主要按鈕滑入狀態。 |
| 主色淺底 | `--soft-primary: #eef0ff` | 輕量選取底色與主色提示。 |
| 成功 | `--success: #1d9561` | 成功與就緒狀態。 |
| 警告 | `--warning: #b87918` | 待處理或需要注意的狀態。 |
| 危險 | `--danger: #bd4b55` | 錯誤、刪除及破壞性操作。 |
| 側欄背景 | `#18243d` | 主導覽底色。 |
| 側欄選取 | `#2c3d67`、左側 `#8c98ff` 標記 | 同時以底色、文字與標記表達目前位置。 |

狀態應搭配文字或圖示，不得只靠顏色區分。專案自訂色用於識別專案，不取代成功、警告或錯誤的語意。新配色須確認文字與背景對比，不因沿用色碼就假設所有組合都符合無障礙需求。

## 3. 字體與文字層級

介面字體使用 `"Segoe UI", "Noto Sans TC", sans-serif`；Markdown 原始碼與程式內容使用 `Consolas, "Noto Sans Mono", monospace`。表單控制項繼承介面字體。

| 層級 | 既有尺寸基準 | 用途 |
| --- | --- | --- |
| 頁面標題 `h1` | 29px | 每頁主要標題，保持簡潔。 |
| 區段標題 `h2` | 19px | 面板及主要內容分組。 |
| 子標題 `h3` | 15px | 面板內的小節。 |
| 側欄項目 | 14px | 功能導覽。 |
| 按鈕與一般操作資訊 | 13px | 控制項、列表與提示。 |
| 欄位標籤 | 12px、700 字重 | 說明輸入用途。 |
| 輔助 metadata | 11–12px | 日期、版本與補充資訊。 |
| 統計數字 | 一般 27px，今日工作台 23px | 突出數值，搭配名稱與單位。 |

段落行高以 1.65 為基準；工作摘要閱讀區沿用較寬鬆的 14px／1.9。上述是既有層級，不是要求把所有正文縮為 13px。新增關鍵資訊不可沿用月曆等緊湊元件的小字特例。

介面文案使用繁體中文（台灣），按鈕以具體動詞命名，例如「儲存」、「驗證來源」、「重新產生」。說明先寫使用者需要知道的結果與下一步；技術細節放在可展開的診斷區。錯誤訊息應說明失敗動作與可採取的處理方式。

## 4. 頁面骨架與間距

桌面以左側固定導覽搭配右側主內容。沿用 `.app-shell`、`.sidebar`、`.main-content` 與 `.content-container`，不要在頁面重建另一套全站骨架。

| 項目 | 目前基準 |
| --- | --- |
| 桌面側欄 | 248px 寬；可獨立垂直捲動。 |
| 桌面內容 | 最大 1180px，寬度 `min(1180px, calc(100% - 40px))`。 |
| 內容上下留白 | 上 20px、下 72px。 |
| 頁面標題區 | `.page-heading`，標題與操作分列，區塊下方 28px。 |
| 一般面板 | `.panel`，內距 23px、圓角 14px、1px 邊框。 |
| 面板陰影 | `--shadow: 0 12px 30px rgba(50, 68, 110, .07)`。 |
| 面板／工作區間距 | 常用 18px；緊湊紀錄 stack 使用 14px。 |
| 表單欄位間距 | 16px；標籤與輸入框 6px。 |
| 按鈕列 | 9px 間距，允許換行。 |

同頁多面板由頁面層級的 grid／stack 和 `gap` 管理。CSS 雖有 `.panel + .panel`，新增或調整版面不得只依賴它：面板被條件區塊或其他容器包住時可能失去間距，也可能與 gap 重複累加。

表單操作列後若會條件顯示進度、錯誤或提示卡，表單與該卡須放入同一個頁面範圍的 stack，並以 `gap` 管理兩者間距。不要把卡片直接接在 `.form-grid` 外、也不要用共用進度元件的外距補救；同一種進度元件在來源卡、面板或對話框中需要不同的容器間距。

建議沿用以下結構概念，class 名稱依頁面命名：

```css
.example-page-stack {
    display: grid;
    gap: 18px;
    min-width: 0;
}

.example-page-stack > .panel {
    margin-top: 0;
}
```

雙欄區域使用 `minmax(0, ...)`，長標題、路徑與內容允許換行，避免撑開容器。閱讀型報告頁可沿用 `.report-page-stack` 的無陰影、12px 圓角特例；不要將此特例擴大成全站樣式變更。

今日工作台依內容區實際寬度自動排列：每欄至少 510px，兩欄加上 18px 間距需要 1038px，不足時改為上下排列。快速填寫面板使用 inline-size container query；內部不足 472px 時，日期、時數與專案改為單欄，標題操作允許換列但按鈕文字不拆行，避免編輯器隨表單軌道溢出。

## 5. 共用元件規範

### 按鈕與連結

- `.button`：最小高度 38px、水平內距 15px、圓角 8px、13px 字體。
- `.button-primary`：靛藍實底，用於本區最重要的提交或產生動作。
- `.button-secondary`：白底細框，用於取消、驗證及其他次要操作。
- `.button-danger`：淡紅底與紅色文字／邊框，用於刪除等危險動作。
- `.button-small`：31px 高、12px 字體，限列表內緊湊操作；行動版應確認點擊範圍與間距。
- 停用狀態使用原生 `disabled`，沿用 `.55` 透明度與禁止游標；執行中同時呈現動作狀態。
- 導覽使用連結，執行動作使用按鈕；純圖示按鈕需有可辨識的 accessible name。

### 捉迷藏小貓

共用版型在 `.content-container` 內提供靛藍線條、淡紫底的 SVG 小貓，使用既有 `--primary`、`--soft-primary`、`--panel` 與 `--line`。活動範圍為內容容器與可見視窗的交集，保留裝置安全區，不進入側欄或內容外的留白。88px 容器內裁切圖案，露出約半顆頭；點擊範圍維持 88px，層級 100，低於通知、對話框與處理遮罩。隱藏不占頁面空間。

首次互動載入後 5 秒出現，桌面與手機都從內容可見區的上、下、左、右邊緣選位，每條邊以不超過 88px 的間距建立候選位置；上方小貓倒掛探頭，下方小貓向上探頭。選位避開可操作與編輯區域，優先換方向，再在該方向隨機選位，避免候選點數較多的邊緣占優勢。無安全位置時等待 10 秒重試。停留 30～60 秒後縮回，間隔 5～10 秒換位；不連續使用同一位置、不跨畫面移動。滑鼠靠近、鍵盤聚焦及閱讀泡泡時暫停換位；內容區不在視窗內、可見範圍不足、背景分頁及遮罩期間隱藏並暫停。切換路由保持同一元件狀態。

對話泡泡為白底、24px 圓角、細邊框與柔和陰影，附貓掌標記、「小貓的悄悄話」標題及關閉入口。正文使用 17px／1.85 行高；「收到，謝謝你」為主色按鈕，「再來一句」為淡色按鈕，休息操作使用次要文字。泡泡最多 336px，距內容可見範圍至少 12px；依探頭方向朝內展開，尾巴指向小貓，空間不足時換邊或縮短可捲動區。極短視窗以內容可達性為優先，不顯示無法指向小貓的尾巴。捲動容器與外層尾巴分離，操作按鈕至少 44px 高。

出場使用 1.8 秒的「偷看、縮回一點、再探頭」動作，沿目前方向移動且始終裁切在原本 88px 範圍內；靠近 0.2 秒、泡泡展開 0.3 秒、收起 0.4 秒。等待時每隔 10～20 秒隨機眨眼動耳朵、左右張望或歪頭，不連續重複同一動作。被找到時露出笑眼、揮爪 1 秒並短暫閃出小星光，之後靜止讓使用者閱讀。靠近或鍵盤聚焦立即取消偷看與等待動作，不會在準備點擊時逃走；背景、遮罩與休息也清除表演狀態，不另加動畫計時器。

「減少動態效果」停用所有表演、動畫與自動換位，保留固定在邊緣、只露出約半顆頭的入口及靜態笑眼。Escape 與「收到，謝謝你」關閉泡泡並返回入口焦點，焦點離開後才躲藏；「本次先休息」在同一分頁的 sessionStorage 記錄暫停，新分頁恢復。句子以一般文字與禮貌朗讀呈現，不自動消失；句庫只有一條時不顯示換句操作。

### 表單與編輯器

使用 `.form-grid`、`.field` 與 `.full`，桌面一般兩欄，長內容跨欄。欄位底色 `#fbfcff`、邊框 `#d6ddea`、圓角 8px，輸入內距約 10px 11px；select 最小高度 40px。

標籤持續可見，不以 placeholder 取代。說明與驗證訊息靠近欄位，錯誤時保留已輸入內容。focus 沿用 `#8794ec` 邊框與 3px 淺靛藍外圈，不可移除焦點而不提供替代樣式。

Markdown 編輯沿用現有 EasyMDE 工具列與編輯器整合：一般區域最小高度 280px，今日快速填寫為 125px，彈窗編輯器為 240px。依任務選用既有變體，保留垂直閱讀與調整空間。

### 卡片、列表與狀態

`.panel-heading` 統一標題、補充說明和區段操作。統計卡以名稱、數字、輔助說明排序；只有可導覽的卡片才呈現互動 hover／focus。

來源、工作紀錄及報告列表沿用既有卡片與列樣式，先顯示標題及狀態，再顯示時間等 metadata。大量次要操作收進更多選單。唯讀、停用、進行中、過期與失敗需有清楚文字，不只變淡或換色。

空資料沿用 `.empty-state` 的白底、虛線框及置中內容，說明目前為何沒有資料，並在適用時提供新增或設定入口。篩選無結果與尚未建立資料應使用不同文案。

### 導覽、分頁與月曆

側欄沿用品牌圖與功能分組，active 狀態使用較亮底色及左側標記。品牌資產重用 `wwwroot/brand-mark.png`，避免在頁面自行繪製另一個版本。

來源分頁沿用 `.source-workspace-tabs`，選中項為白底主色文字；切換按鈕與計數採既有樣式。歷史列表／月曆沿用現有檢視切換，不另造不同的選取語言。月曆需保留日期、選取狀態與工作摘要資訊，窄畫面可在月曆容器內水平捲動。

### 對話框、通知與進度

年度回顧的每月涵蓋、成果明細、成果編輯與機敏預覽使用對話框，主頁保留統計、精簡列表及草稿。`AnnualReviewDialog` 使用原生 modal dialog 管理焦點限制與 Escape 關閉，關閉後返回觸發控制項；手機沿用底部面板。面板由 18px stack gap 管理，成果列表超過 420px 時在列表內捲動。資料讀取、儲存與 AI 生成須呈現階段文字並停用重複操作；表單失敗時保留輸入。

沿用 `ConfirmDialog`、`ToastContainer`、`OperationProgress` 與機敏預覽元件，避免各頁重複實作同類互動。

桌面 `.modal-dialog` 最大寬度 620px，寬版 820px；內距 24px、圓角 16px，內容可捲動。遮罩採半透明深藍與輕微模糊。標題在上、關閉按鈕在右上、操作列在下方；破壞性動作需清楚顯示影響與確認動詞。

今日頁面的工作紀錄與參考資料編輯視窗，表單內容在視窗內獨立捲動，儲存、捨棄與關閉操作列固定顯示在底部。視窗高度不超過可見區域；手機仍為底部面板，貼上長篇內容或展開錯誤訊息時也能直接操作儲存。

Toast 使用成功、資訊與錯誤層級，保留文字、圖示與關閉入口；延續 `aria-live` 及錯誤 `role="alert"`。重要待處理事項不可只依賴會消失的通知。

長時間操作沿用 `OperationNotification` 呈現保持功能頁開啟的說明與選用桌面通知引導；通知偏好沿用 `NotificationPreferences`，以 12px grid gap 配置文案及可換行按鈕列，隱藏時不保留空白。處理結果以固定分頁標題提示，不持續閃爍；只有非前景頁面才發桌面完成通知，取消與等待機敏確認不視為完成。AI 覆蓋卡加入通知引導後須可在 92vh 內捲動，避免窄螢幕或較矮視窗截斷按鈕。通知設定頁重用 panel、button 與狀態文字；權限提示只由使用者點擊觸發。

長時間操作顯示目前階段與進度，尚無確定百分比時使用不定進度呈現。只有實際可取消的操作顯示取消入口；避免無限旋轉但沒有文字。動畫沿用短促、低干擾的風格，新增動效須尊重 `prefers-reduced-motion`。

年度草稿沿用 `OperationProgress` 中央載入卡與不定進度動畫，準備及 AI 整理期間提供「中斷」。點擊後改為「正在中斷…」並停用按鈕，等待作業收尾才關閉；遮蔽確認期間不顯示載入卡。中斷保留原草稿，不顯示失敗通知。

## 6. 響應式規則

沿用既有 breakpoint，依內容需要補充局部調整，不任意建立全站第二套斷點。

| 範圍 | 既有行為與維護要求 |
| --- | --- |
| 大於 900px | 248px 固定側欄，工作台與設定可使用雙欄。 |
| 900px 以下 | 側欄縮至 205px；主要工作區與設定雙欄轉單欄，統計卡通常兩欄。 |
| 760px 以下 | 歷史月曆容器可水平捲動，避免把七日資訊擠至無法閱讀。 |
| 650px 以下 | 側欄轉精簡頂列，保留品牌與至少 44px 高的選單按鈕；導覽預設收合，展開後兩欄排列，點選導覽或按 Escape 收合，Escape 返回按鈕焦點。按鈕提供 aria-expanded 與 aria-controls，收合連結不可取得焦點；桌面始終顯示導覽。內容左右各 12px；標題與操作上下排列，表單轉單欄。 |
| 650px 以下的對話框 | 改為底部全寬面板、上方圓角，最大高度 92vh，內容可捲動。 |
| 500px 以下 | 報告工具列等密集操作依既有局部規則重排。 |
| 桌面高度 800px 以下 | 收緊側欄垂直間距，保留捲動及底部資訊可達性。 |

以 320px 作為目前 body 最小寬度基準。窄螢幕按鈕可換行或等分寬度，不應被截斷；僅必要的月曆或程式內容採局部橫向捲動，避免整頁水平溢出。

## 7. 可用性與無障礙要求

以下為新增與修改介面時的驗收要求，不代表既有全站已完成無障礙認證。

- 維持清楚的標題層級、表單 label，以及 link／button 的正確語意。
- 所有互動控制可透過鍵盤操作，focus-visible 清楚可見；不可將 `h1` 路由焦點的特殊規則擴用到按鈕。
- 對話框有標題及 dialog 語意；修改時驗證焦點進入、關閉與返回觸發處的流程。
- 圖示搭配文字或 accessible name；裝飾圖示不重複朗讀。
- 狀態不只用色彩；錯誤、成功與進度應可被輔助技術辨識。
- 支援長文字、空資料、載入、失敗、停用及條件面板展開等狀態，避免版面跳動與遮擋操作。
- 新增動畫減少大幅位移，提供減少動態效果的處理。

## 8. 修改與驗收流程

### 新畫面的設計決策範例

| 情境 | 採用方式與原因 | 避免的做法 |
| --- | --- | --- |
| 新增設定頁 | 沿用頁面標題、白色面板、欄位分組與主要儲存動作，讓使用者延續既有操作習慣。 | 每一個設定區塊使用不同品牌色或不同按鈕造型。 |
| 編輯與閱讀同時存在 | 編輯器使用既有工具列及等寬原始碼樣式，閱讀正文使用一般字體與較寬行高。 | 將整份工作摘要呈現成小字程式碼區塊。 |
| 多個獨立操作區 | 每區突出最重要的動作，次要操作降低視覺強度；全頁仍維持明確的主要任務。 | 將驗證、取消、刪除及儲存全部做成實底主色按鈕。 |
| 條件面板展開 | 由共同 stack 的 gap 維持間距，檢查展開前後及窄畫面。 | 只在某一塊面板加 margin，造成其他組合黏連或雙倍空白。 |
| 表單後顯示進度 | 表單與條件式進度卡置於同一個區域 stack，以 18px gap 保留操作與狀態的分隔。 | 將進度卡直接放在 `.form-grid` 後方，或修改共用進度卡的 margin 影響其他頁面。 |
| 尚未有工作紀錄 | 說明尚無紀錄，提供新增入口，保留工作區的視覺結構。 | 只顯示空白或將正常的空資料標為紅色錯誤。 |
| 來源收集失敗 | 在該來源附近顯示失敗文字與可行的下一步，維持其他內容可讀。 | 只將狀態圓點變紅，或用全頁遮罩阻擋無關操作。 |

給設計或實作工具的頁面描述可採以下格式，並補上具體功能需求：

> 延續 WorkLens 的深藍側欄、冷灰藍底與白色工作面板。頁首先呈現標題及簡短說明，主要內容依任務分組，使用柔和圓角、細邊框與輕陰影。以靛藍突出主要動作，正文保持深藍灰並優先保留閱讀空間。重用現有元件與樣式數值，窄畫面依既有斷點轉為單欄，並完整呈現空資料、處理中及失敗狀態。

### 驗收步驟

1. 修改前先查閱本文件、相關 Razor 元件及 `wwwroot/app.css`，選擇最接近的既有頁面作為參考。
2. 優先使用既有 CSS 變數、元件與語意 class；頁面特例以頁面範圍 class 限定，避免改動全域 selector 影響其他頁。
3. 至少檢查桌面、900px 附近、650px 附近與 320px 窄畫面；側欄相關變更另外檢查較矮視窗。
4. 檢查 hover、focus、disabled、執行中、錯誤、空資料與條件區塊顯示／隱藏；涉及表單時確認未儲存內容不因呈現調整而遺失。
5. 涉及布局規則時執行相關 layout 回歸測試，並以實際畫面確認間距、溢出與可讀性；不能只用 class 存在的斷言取代視覺檢查。PR 的版面變更附上截圖，避免包含私人工作資料。
6. 配色、字體層級、共用元件外觀、版面尺寸或響應式規則若有改變，在同一變更中更新本文件相應章節，保持規範與 CSS 一致。

## 9. 編寫依據

以下來源提供文件組織方法，WorkLens 的實際配色、尺寸與元件仍以本專案為準。查閱日期：2026-09-13。

- [Google Labs：DESIGN.md 指引](https://github.com/google-labs-code/stitch-skills/blob/main/plugins/stitch-utilities/skills/design-md/SKILL.md)：以整體氛圍、色彩角色、字體、元件及布局組織設計語言，結合視覺描述與精確數值，說明設計用途和原因。
- [Google Labs：從前端程式抽取設計規範](https://github.com/google-labs-code/stitch-skills/blob/main/plugins/stitch-design/skills/extract-design-md/SKILL.md)：由樣式與元件辨識可重用模式，涵蓋互動狀態、留白與響應式行為，避免只輸出 CSS 屬性清單。
- [Atlassian：Design tokens](https://atlassian.design/tokens/design-tokens)：以命名 token 集中保存設計決策；本專案對應到既有 CSS 變數，未定義的尺寸不假稱已有 token。
- [Carbon：Button usage](https://carbondesignsystem.com/components/button/usage/)：元件文件應解釋用途、變體與動作層級。WorkLens 依多工作區情境制定操作區層級規則，並未直接照搬 Carbon 的全套按鈕限制。

本文件採用這些來源的編寫原則；Stitch 的專案識別、匯入用 YAML 及工具操作流程屬於其整合格式，不是本專案一般 Markdown 指引的必要內容。
